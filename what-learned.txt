1. je component a firebase dorkar sai component a firebase configure corbo and 
use korbo.

2. if we want to authenticate use with Google then we need to import {} 
GoogleAuthProvider class from firebase/auth. then we need to create a object 
by using new keyword under component function.
    ==> import {GoogleAuthProvider} from 'firebase/auth';

    function App(){
        const provider = new googleAuthProvider();
        return(
            <div>
                <button>Sign in</button>
            </div>
        );
    }